import { Meta, StoryObj } from '@storybook/angular';
import { XComponent } from './x.component';

const meta: Meta<XComponent> = {
  title: 'Icons/X',
  component: XComponent,
};

export default meta;

type Story = StoryObj<XComponent>;

export const Default: Story = {};
