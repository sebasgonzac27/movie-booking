import { Meta, StoryObj } from '@storybook/angular';
import { MinusComponent } from './minus.component';

const meta: Meta<MinusComponent> = {
  title: 'Icons/Minus',
  component: MinusComponent,
};

export default meta;

type Story = StoryObj<MinusComponent>;

export const Default: Story = {};
